services:
  nfs-server:
    image: pedroetb/nfs-server
    volumes:
      - /data/nfs:/nfs
    environment:
      - NFS_EXPORT_0=/nfs *(rw,sync,no_subtree_check,fsid=0)
    cap_add:
      - SYS_ADMIN
    ports:
      - 2049:2049/tcp
      - 2049:2049/udp

# TODO: maybe add samba: https://github.com/dperson/samba
# https://github.com/dperson/samba/blob/master/docker-compose.yml
